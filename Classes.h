class Circle {
private:
    // Member variable
    double radius;

public:
    // Constructor (a special member function)
    Circle(double r);

    // A member function (or method) to set the radius
    void setRadius(double r);

    // A member function (or method) to calculate the area
    double calculateArea();
};